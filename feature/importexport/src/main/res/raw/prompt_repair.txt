你之前根据我的课表截图生成过一段 JSON，但我的课表 App 导入时报了下面这些错误。请修正后重新输出。

## 错误信息
{ERRORS}

## 你上次的输出
{ORIGINAL}

## 修正要求
1. 只输出修正后的完整 JSON，不要输出任何解释、前言、后语、总结。用户会把你的回复整段复制粘贴进 App。
2. 针对上面每一条错误逐一修正，其余内容保持不变。
3. 如果某个字段从截图里看不出来，填 null，不要编造。
4. format、version、term 三项必须与上次保持一致。
5. 输出前重新检查：dayOfWeek 是否都在 1-7、endSection 是否都大于等于 startSection、weeks 是否只含数字和短横线逗号单双。
