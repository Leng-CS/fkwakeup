# 课表识别提示词（App 内置文案源文件）

> 本文是 App 内置提示词的**唯一来源**。
> 工程实现时把「二、提示词正文」与「三、修复版提示词」分别存为
> `app/src/main/res/raw/prompt_import.txt` 与 `app/src/main/res/raw/prompt_repair.txt`，
> 文件内容与本文对应章节**逐字一致**（含换行与缩进）。
> **修改本文必须同步修改 raw 资源。**

---

## 一、用户在 App 内看到的引导文案（strings.xml 来源）

### 导入页 · Tab「AI 识别建课表」

```
第 1 步　去教务系统截一张完整的课表
　　　　　· 要能看到星期表头和左侧节次列，别只截课程格子
　　　　　· 课表太长就分两次截，一次识别半张
　　　　　· 避免模糊、反光、只截一半

第 2 步　复制提示词，连截图一起发给你常用的 AI

第 3 步　把 AI 的回复整段复制，回到本页粘贴

第 4 步　确认识别结果，微调后导入
```

### 复制区

| 元素 | 文案 |
|---|---|
| 主按钮 | `复制提示词` |
| 复制后 Snackbar | `提示词已复制，去粘贴给 AI 吧` |
| 按钮下方小字 | `支持 ChatGPT / Claude / 豆包 / 通义 / Gemini 等任意 AI` |
| 折叠区标题 | `查看完整提示词（可手动复制）` |

### 粘贴区

| 元素 | 文案 |
|---|---|
| 输入框占位符 | `把 AI 的回复整段粘贴到这里，不用清理 ```json 标记` |
| 辅助按钮 | `从剪贴板粘贴` / `清空` / `开始解析` |
| 空状态 | `还没有内容？先复制提示词去问 AI` |

### 导入失败页

| 元素 | 文案 |
|---|---|
| 标题 | `没能识别出课表` |
| 正文 | `第 3 条记录的 dayOfWeek 是 9，应为 1-7` |
| 按钮 1 | `复制错误报告，让 AI 重新生成` |
| 按钮 2 | `手动修改` |
| 按钮 3 | `换一张截图重来` |

### 首次导入成功后

引导卡片：`课表已生成 · 长按桌面添加小组件，上课前一眼看到`

---

## 二、提示词正文（App 内「复制提示词」按钮复制的就是这一段）

````
你是一个课程表信息提取器。我会给你一张大学教务系统的课程表截图，你要把它转换成一个严格格式的 JSON 文件。

## 输出硬性要求（违反任何一条都是失败）

1. 只输出一个 JSON 对象，**不要输出任何解释、前言、后语、总结**。用户会把你的回复整段复制粘贴进一个 App，任何多余文字都会导致导入失败。
2. JSON 必须放在 ```json 和 ``` 之间。
3. 不要编造信息。看不清或不确定的字段填 null，并在 note 字段里说明原因。
4. 每一行代表**一个上课时间段**。同一门课如果在多个时间段上课，就写成多行（不要合并、不要嵌套）。
5. 请原样保留课程名称，不要缩写、不要翻译、不要加空格。

## JSON 结构

{
  "format": "campus-timetable",
  "version": "1.0",
  "term": {
    "name": "学期名称，例如：2026-2027 秋季学期",
    "startMonday": "学期第一周的周一，格式 YYYY-MM-DD；图中没有就从当前日期合理推断",
    "totalWeeks": 18
  },
  "sessions": [
    {
      "name": "课程名称",
      "teacher": "任课教师，没有就填 null",
      "location": "上课地点，没有就填 null",
      "dayOfWeek": 1,
      "startSection": 1,
      "endSection": 2,
      "weeks": "1-16",
      "note": null
    }
  ]
}

## 字段填写规则

- dayOfWeek：1=周一，2=周二，3=周三，4=周四，5=周五，6=周六，7=周日。只能是 1 到 7 的整数。
- startSection / endSection：第几节课，从 1 开始，含首尾。只上一节课时两者相同（如 3 和 3）。连上两节写 1 和 2。
- weeks：上课周次表达式，字符串，规则如下：
  - 连续周：`1-16`
  - 跳过的周：`1-9,11-18`（表示第 10 周不上课）
  - 单周：`1-16单`；双周：`2-16双`
  - 离散周：`1,3,5,7`
  - 全程上课但不知道周数：用 `1-18`（与 totalWeeks 保持一致）
- teacher / location：不知道就填 null，不要填"未知""待定""无"这类文字。
- note：只在有需要说明的情况填写，否则填 null。

## 常见情况处理

- 同一门课一周上多次 → 写多行，每行 dayOfWeek 不同。
- 同一门课同一天不同节次 → 写多行。
- 课程分单双周在不同教室 → 写多行，周次分别写 `1-16单` 和 `1-16双`。
- 课表里有"实践周""军训""考试周"等非课程条目 → 照常作为一条 session 输出，name 原样保留。
- 图片里的课程跨了多节 → startSection 写最小节次，endSection 写最大节次。

## 输出前自检（逐条确认）

1. 我是否只输出了 JSON，没有任何多余文字？
2. format 是否恰好是 "campus-timetable"？version 是否恰好是 "1.0"？
3. 每个 session 的 dayOfWeek 是否都在 1-7 之间？
4. 每个 session 的 endSection 是否都大于或等于 startSection？
5. 每个 session 的 weeks 是否只使用了数字、短横线、逗号、"单"、"双"这几种字符？
6. 我不确定的字段是否都填了 null 而不是编造内容？
7. 同一门课的多个时间段是否写成了多行而不是嵌套结构？
8. JSON 是否合法（没有尾随逗号、所有字符串用双引号）？

现在开始，这是课表截图：
````

---

## 三、修复版提示词（导入失败时复制给 AI）

存为 `res/raw/prompt_repair.txt`。运行时把 `{ERRORS}` 替换为错误列表、把 `{ORIGINAL}` 替换为用户原始文本。

````
你之前根据我的课表截图生成过一段 JSON，但我的课表 App 导入时报了下面这些错误。请修正后重新输出。

## 错误信息
{ERRORS}

## 你上次的输出
{ORIGINAL}

## 修正要求
1. 只输出修正后的完整 JSON，不要输出任何解释、前言、后语、总结。用户会把你的回复整段复制粘贴进 App。
2. 针对上面每一条错误逐一修正，其余内容保持不变。
3. 如果某个字段从截图里看不出来，填 null，不要编造。
4. format、version、term 三项必须与上次保持一致。
5. 输出前重新检查：dayOfWeek 是否都在 1-7、endSection 是否都大于等于 startSection、weeks 是否只含数字和短横线逗号单双。
````

**错误列表示例**（App 生成，每条一行，供 AI 直接对照）

```
第 3 条记录：dayOfWeek 值为 9，应为 1-7
第 7 条记录：周次"1-16周（单）"无法识别
第 12 条记录：endSection 3 小于 startSection 5
```

**拼接规则**：`prompt_repair.txt` → 替换 `{ERRORS}` → 替换 `{ORIGINAL}` → 复制到剪贴板。原始文本超过 8 KB 时截断并追加 `\n...（内容过长已截断）`。

---

## 四、如果 AI 输出不合格怎么办

| 现象 | 处理 |
|---|---|
| AI 在 JSON 外面加了说明文字 | 没关系，App 会自动提取 JSON 部分 |
| AI 输出被截断 | 让 AI 继续输出，或只截图半张课表分两次识别 |
| AI 把周次写成了"1-16周（单）" | App 会尝试归一化；失败时用修复版提示词让 AI 重生成 |
| 识别结果明显错得离谱 | 换一个模型重试，或在提示词末尾补一句具体纠错要求 |
| 重复失败 | 点「换一张截图重来」，大概率是截图质量的问题而不是提示词的问题 |

---

## 附录：字段速查

| 字段 | 取值 | 例子 |
|---|---|---|
| dayOfWeek | 1-7 | 3（周三） |
| startSection | ≥1 整数 | 1 |
| endSection | ≥startSection 整数 | 2 |
| weeks | 表达式 | `1-16` / `1-16单` / `2-16双` / `1-9,11-18` / `1,3,5` |
| teacher / location | 字符串或 null | `"教三-301"` |
